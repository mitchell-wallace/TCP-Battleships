namespace Battleships{

    public class OpponentConnection{

        public static bool TryHit(int column, int row) { // TODO
            if (column == 2) return true;
            return false;
        }

    }
}